/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/pages/*.html'],
    theme: {
        extend: {},
        fontFamily: {
            sans: ['Ubuntu','sans','sans-serif']
        }
    },
    plugins: [],
}